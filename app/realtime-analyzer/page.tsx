/**
 * Realtime Analyzer Demo Page
 * 
 * Shows the complete "director's feedback" experience with live streaming
 * updates as the analysis progresses through the storyboard flow.
 */

import RealtimeAnalyzerDemo from "@/components/analyzer/RealtimeAnalyzerDemo";

export default function RealtimeAnalyzerPage() {
  return (
    <div className="min-h-screen">
      <RealtimeAnalyzerDemo />
    </div>
  );
}
